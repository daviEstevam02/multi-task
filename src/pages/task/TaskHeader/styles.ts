import styled from "styled-components";

const Container = styled.div`
`
const Content = styled.div`
    width: 100%;
`
const TaskDescription = styled.div`
`
const TaskNumber = styled.h2`
`

const TaskTitle = styled.input`
    width: 100%;
    padding: 2;
`

const TaskAssigned = styled.input``

export { 
    Container,
    Content,
    TaskAssigned,
    TaskDescription,
    TaskNumber,
    TaskTitle,
}